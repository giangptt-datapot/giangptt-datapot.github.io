# Requirements (Sample)

We want to migrate 2 blog posts from datapot.vn into dev site.

Deliverables:
- 2 migrated posts available on dev
- Migration notes (source -> target slug, issues)
- Prompt pack to rerun migration for new posts

AC:
- Content/format matches source >= 95% (heading/list/table/code)
- Images ok (no 404), links not broken
